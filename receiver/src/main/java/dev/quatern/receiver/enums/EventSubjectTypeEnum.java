package dev.quatern.receiver.enums;

public enum EventSubjectTypeEnum {
    ORDER
    // Aqui seriam declarados os outros domínios que devem ter atualizações sobre suas entidades registradas no Receiver
}
